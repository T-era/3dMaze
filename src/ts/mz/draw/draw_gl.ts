import { Common } from '../../common';

import { Types } from '../types';
import { Field } from '../field';


export function initGl(canvas :HTMLCanvasElement, gl :WebGLRenderingContext) {
	return null;
}
